// Configuration - update this for your deployment
window.WEBCALL_CONFIG = {
  // Backend server URL (no trailing slash)
  // Always point to the backend server regardless of where admin is hosted
  API_URL: "https://webcall2backendserver.onrender.com",
};
